<%# notice that u need @charities to render this component %>
<div class="px-5 pt-5 mt-5">
    <div class="row px-1 pb-5 justify-content-left">
      <% charities.each do |charity| %>

         <%= render "shared/single_charity_card", charity: charity %>

      <% end %>
    </div>
</div>
